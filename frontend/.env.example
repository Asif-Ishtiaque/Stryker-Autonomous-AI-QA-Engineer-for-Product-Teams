# Base URL of the Stryker FastAPI backend (REST). All routes are prefixed /api/v1.
NEXT_PUBLIC_API_URL=http://localhost:8000

# Base URL of the backend's WebSocket server, used for live run execution events
# at ${NEXT_PUBLIC_WS_URL}/api/v1/ws/runs/{run_id}.
NEXT_PUBLIC_WS_URL=ws://localhost:8000
